spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/librarydb
    username: postgres
    password: "10140019"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

server:
  port: 8085

jwt:
  secret: "secret-key-should-be-long-and-secure-123456"
  expiration: 86400000 # 1 gün = 24 * 60 * 60 * 1000 ms
